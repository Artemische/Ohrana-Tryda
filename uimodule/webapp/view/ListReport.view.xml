<mvc:View controllerName="bntu.ohranaTryda.controller.ListReport" height="100%"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:layout="sap.ui.layout"
    xmlns="sap.m"
    xmlns:f="sap.f"
    xmlns:fb="sap.ui.comp.filterbar"
    xmlns:vm="sap.ui.comp.variants"
    xmlns:app="http://schemas.sap.com/sapui5/extension/sap.ui.core.CustomData/1">
    <f:DynamicPage id="dynamicPageId" headerExpanded="{/headerExpanded}" class="sapUiNoContentPadding">
        <!-- DynamicPage Title -->
        <f:title>
            <f:DynamicPageTitle>
                <f:heading>
                    <vm:VariantManagement standardItemText="{i18n>variantDefaultTxt}">
                    </vm:VariantManagement>
                </f:heading>
                <f:actions>
                    <ToolbarSpacer/>
                    <Button text="{i18n>loginButton}" type="Emphasized" press="openLoginDialog" />
                </f:actions>
            </f:DynamicPageTitle>
        </f:title>
        <!-- DynamicPage Header -->
        <f:header>
            <f:DynamicPageHeader pinnable="true">
                <f:content>
                    <fb:FilterBar id="filterbar" reset="onReset" search="onSearch" useToolbar="false" showGoOnFB="true">
                        <fb:filterItems>
                            <fb:FilterItem name="A" label="{i18n>nameFltrLbl}">
                                <fb:control>
                                    <Input id="nameFltr" app:prop="name" change="onFilterChange" />
                                </fb:control>
                            </fb:FilterItem>
                            <fb:FilterItem name="B" label="{i18n>sNameFltrLbl}">
                                <fb:control>
                                    <Input id="secondNameFltr" app:prop="secondName" change="onFilterChange" />
                                </fb:control>
                            </fb:FilterItem>
                            <fb:FilterItem name="C" label="{i18n>departmentFltrLbl}">
                                <fb:control>
                                    <Select id="departmentFltr" app:prop="department" change="onFilterChange" forceSelection="false" items="{
                                            path: '/departments'
                                        }">
                                        <core:Item key="{key}" text="{name}"/>
                                    </Select>
                                </fb:control>
                            </fb:FilterItem>
                        </fb:filterItems>
                    </fb:FilterBar>
                </f:content>
            </f:DynamicPageHeader>
        </f:header>

        <!-- DynamicPage Content -->
        <f:content>
            <Table id="usersTableId" 
                inset="false" 
                items="{
                    path: '/Users'
                }" 
                class="sapUiResponsiveMargin"
                noDataText="{i18n>noUsersText}" 
                width="auto">
                <headerToolbar>
                    <OverflowToolbar>
                        <ToolbarSpacer/>
                        <Button
                            text="{i18n>createUserButton}"
                            visible="{/ActiveUser/isAdmin}"
                            press=".onCreateUserPress"/>
                    </OverflowToolbar>
                </headerToolbar>
                <columns>
                    <Column minScreenWidth="Phone" demandPopin="true">
                        <Text text="{i18n>nameColumn}"/>
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>sNameColumn}"/>
                    </Column>
                    <Column minScreenWidth="Tablet" demandPopin="true">
                        <Text text="{i18n>departmentColumn}"/>
                    </Column>
                    <Column minScreenWidth="Phone" demandPopin="true">
                        <Text text="{i18n>atestationColumn}"/>
                    </Column>
                </columns>

                <items>
                    <ColumnListItem type="Navigation" vAlign="Middle" press="onLineItemPress" >
                        <cells>
                            <Text text="{name}"/>
                            <Text text="{secondName}"/>
                            <Text text="{department}"/>
                            <ObjectStatus 
                                text="{= ${isAttestationPassed} ? 'Сдано': 'Не сдано'}" 
                                state="{= ${isAttestationPassed} ? 'Success': 'Error'}" />
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </f:content>

        <!-- DynamicPage Footer -->
        <f:footer>
            <OverflowToolbar>
                <ToolbarSpacer/>
                <Button type="Accept" text="Accept"/>
                <Button type="Reject" text="Reject"/>
            </OverflowToolbar>
        </f:footer>
    </f:DynamicPage>
</mvc:View>